# Algomim for Claude Code

Status: **Future**

This directory reserves the `claude-code` integration ID. Claude Code
compatibility, setup artifacts, and installers have not been released.

Do not use this directory for installation until its status is promoted to
`pilot` in the root `integrations.json` file.
